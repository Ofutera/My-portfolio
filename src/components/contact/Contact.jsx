import './contact.scss';

const Contact = (props) => {
    return (
        <div className='contact' id='contact' onClick={props.menuClose}>
            
        </div>
    )
}

export default Contact;