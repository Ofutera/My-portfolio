import './testimonials.scss';

const Testimonials = (props) => {
    return (
        <div className='testimonials' id='testimonials' onClick={props.menuClose}>
        
        </div>
    )
}

export default Testimonials;